### Problem 2

Try to automate the threshold values in the star-finding functions. 
Only for functions where a pixel is judged to contain star data, only when its value is more than (median + tolerance * std dev).

If possible, do the same for the kappa value in kappa-sigma clipping.
